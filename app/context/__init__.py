"""Context layer — where retrieval results become meaning.

The five-step process lives here: curation (builder reaching into Data via
Retrieval), synthesis and consolidation (the clocks), prioritization (INDEX and
builder ranking), and intelligent storage (the git-backed Markdown store + the
knowledge graph). Inference is NOT here — Sia serves context to external harnesses.
"""
